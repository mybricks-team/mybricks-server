const services = module.exports = {};

services.uploadImage = function () { }


/**
 * 发布组件库
*/
services.publishComlib = function (zipFile) {
  return new Promise((resolve, reject) => {

    

  });
  // zip 文件持久化
  // 解压 zip 文件
  // 更新数据库版本
}